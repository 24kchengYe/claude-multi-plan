# 复制本文件为 claude-switch.local.sh。本文件默认可以为空：
# Kimi 与 DeepSeek key 都从 ai-api-gateway 解锁文件读取（~/.config/ai-gateway/secrets.env
# 的 KIMI_CODE_API_KEY / DEEPSEEK_API_KEY）。如需显式覆盖（例如用另一枚 key）再取消注释：
# KIMI_KEY="sk-kimi-在这里填你的KimiCodeKey"
# DEEPSEEK_KEY="sk-在这里填你的DeepSeekKey"
